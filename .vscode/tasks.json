{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "windows": {
                "command": "${workspaceFolder}/build_win32.bat",
            },
            "osx": {
                "command": "bash ${workspaceFolder}/build_osx.sh"
            },
            "problemMatcher": [
                "$msCompile"
            ]
        }
    ]
}